<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page.title %> - brutalBaby-web3.0</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">
    <!-- Header -->
    <%- include('partials/header', { user }) %>

    <!-- Main Content -->
    <main class="flex-grow p-4">
        <div class="max-w-2xl mx-auto bg-white shadow-lg rounded-lg p-8">
            <h1 class="text-2xl font-bold mb-4"><%= page.title %></h1>
            <p><strong>Owner:</strong> <%= user.address %></p>
            <p><strong>Created At:</strong> <%= page.createdAt.toLocaleString() %></p>
            <!-- Добавьте дополнительную информацию о странице при необходимости -->
        </div>
    </main>

    <!-- Footer -->
    <%- include('partials/footer') %>
</body>
</html>
